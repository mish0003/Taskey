{% extends "partials/base.html.twig" %}

{% set pageTitle = "Task" %}

{% block content %}
    <h2>Task</h2>
    <p>{{ task.id }}</p>
    <p>{{ task.title }}</p>
    <p>{{ task.description }}</p>
{% endblock %}